[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flake8>=7.3.0",
    "pydantic>=2.13.5",
    "torch>=2.0.0",
    "transformers>=5.16.1",
]
