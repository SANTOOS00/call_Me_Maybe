[project]
name = "call-me-maybe"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flake8>=7.3.0",
    "llm-sdk",
    "mypy>=2.3.1",
    "numpy>=2.4.6",
    "pydantic>=2.13.5",
]

[tool.uv.sources]
llm-sdk = { workspace = true }

[tool.mypy]
python_version = "3.11"
mypy_path = "llm_sdk"

[tool.uv.workspace]
members = ["llm_sdk"]
